# OpenClaw Bootstrap 設定
このディレクトリは `integrations.openclaw.config_dir` として使用されます。
ここに配置された `*.md` ファイルは、各 KUMC OpenClaw ターンの前に
対象の OpenClaw ワークスペースへ同期されます。

## ファイル
- `AGENTS.md`: 互換モードにおけるポリシーレベルの制約
- `SOUL.md`: 実行優先度の高レベル定義
- `USER.md`: 入出力契約およびコマンドマッピングの具体仕様