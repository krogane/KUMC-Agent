
# 指示された際の留意点
- 実装を指示された際、計画の段階で少しでも曖昧な要件がある場合は、実装に移る前にユーザーに質問してください。
- 拡張性・保守性の観点から必要に応じて新規ファイルの作成・削除も行うことができます。
- テストの際、pytestは未導入の環境であることに留意してください。
- 新たなライブラリを導入する際は、requirements.txtに追加してください。

# 設計書ファイル
このレポジトリの詳細設計は docs/design/* を参照してください。ただし、これらの詳細設計を厳密に守る必要はありません。

# CLIや外部連携向けpayloadのschema方針
- payloadのトップレベルには、利用者・連携先が主結果として扱う安定フィールドのみを置いてください。
- 診断情報・内部判断・ルーティング判断・実行モード・デバッグ補助情報は、payload種別にかかわらずすべて`metadata`配下に入れてください。
- 例: `routing_decision`、`fast_mode`、`selected_handler`、`policy_decision`、`trace_id`などはトップレベルへ昇格せず、`metadata`に保持してください。
- 大きな本文断片、検索context、secretを含む可能性がある値など、CLI出力や外部連携に不要または危険なmetadataは出力前に除外・マスクしてください。
- 既存payloadを拡張する場合は、同じ方針に従ってテストとdocs/explanation配下の関連説明も更新してください。

# 各種設定の保存先
- パラメータなどの保存はconfigsフォルダ内に保存する。
- プロンプトはassets/promptsフォルダ内に保存する。
- .env / .env.exampleには、パラメータやプロンプトは保存せず、トークンやAPIキーなどの情報などを保存する。

# 作業の同時進行
- 同じレポジトリ内で、別スレッドのCodexがあなたと同時進行で作業している可能性があります。あなたが関与していないファイルが編集・追加・削除されていてもrollbackなどは行わないでください。